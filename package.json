{
	"name": "await-health",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "bun scripts/build.ts",
		"build:prod": "bun scripts/build.ts --minify",
		"test": "bun test src",
		"typecheck": "tsc --noEmit",
		"fix": "biome check --write src scripts",
		"prepare": "simple-git-hooks"
	},
	"simple-git-hooks": {
		"pre-commit": "bun run fix && git update-index --again"
	},
	"devDependencies": {
		"@await-widget/runtime": "^0.0.17",
		"@biomejs/biome": "^2.4.14",
		"@types/bun": "^1.3.13",
		"simple-git-hooks": "^2.13.1",
		"typescript": "^6.0.2"
	}
}
